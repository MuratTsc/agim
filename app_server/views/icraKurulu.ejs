<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">İcra Kurulu</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->


<div class="row">

    <% if (mesaj == "no") { %>
    <% } else { %>
    <div class="alert alert-success">
    <strong><%= mesaj %></strong> 
    </div>
    <% } %>

</div>

<div class="row">
    <h3>İcra Rapor Listesi</h3>
    <table width="100%" id="dataTable" class="table table-striped table-bordered table-hover">

        <thead>
            <tr>
                <th>Dönem</th>
                <th>Rapor No</th>
                <th>Zümre Başkanı</th>
                <th>Akademik Danışman</th>
                <th>Tarih</th>
            </tr>
        </thead>

        <tbody>
        
            <% for (var i = 0; i < icraRaporlar.length; i++) { %>
                <tr onclick="document.location = '/icraRaporGoruntuleme/<%= icraRaporlar[i].id %>';" style="cursor: pointer;">
                <td><%= icraRaporlar[i].donem %></td>
                <td><%= icraRaporlar[i].raporNo %></td>
                <td><%= icraRaporlar[i].zumreBaskani %></td>
                <td><%= icraRaporlar[i].akademikDanisman %></td>
                <td><%= icraRaporlar[i].raporTarih.getDate() %>.<%= icraRaporlar[i].raporTarih.getMonth()+1 %>.<%= icraRaporlar[i].raporTarih.getFullYear() %></td>
                
                </tr>
            <% } %>
        
        </tbody>

    </table>


</div>
<!-- /.row -->